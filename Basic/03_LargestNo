class Solution {
    public static int largest(int[] arr) {
        // code here
    int x = Integer.MIN_VALUE; //we have intiailise x with the smallest integer no
    for(int i=0; i<arr.length; i++){
        if(x < arr[i]){
            x = arr[i];
        }
    }
    return x;
    }
}
